-- Uncomment this variable and place the .zst file at this path if you ever end up 
-- using a .zst file with custom titlescreen movement
-- title_moves = Other/smwtitledemo.zst

-- Config variables if you want to pass command line options to tools
pixi_options = -l Tools/PIXI/list.txt
-- gps_options = -d
-- addmusick_options = -v
-- uberasm_options = my_list.txt
-- lm_level_import_flags = 0

-- Uncomment this if you want to use globules, see the readme for details on what those are
-- globules_path = Globules